[phases.setup]
nixPkgs = ["go"]

[phases.build]
cmds = [
  "go mod download",
  "npm install",
  "npm run build:css",
  "go run github.com/a-h/templ/cmd/templ@latest generate",
  "go build -o bin/server ./cmd/server"
]

[start]
cmd = "./bin/server"
